const main = () => {
  // if (! user?.quests?.QuestRiftBurroughs) {
  //   return;
  // }

  // const quest = user.quests.QuestRiftBurroughs;

  // const armedBait = quest?.armed_bait || 'disarmed';
  // const canMist = quest?.can_mist || false;
  // const isMisting = quest?.is_misting || false;
  // const mistPercent = quest?.mist_percent || 0;
  // const mistLevel = quest?.mist_released || 0;
  // const mistTier = quest?.mist_tier || 'tier_0';

  // const items = {
  //   brie_string_cheese: quest?.items?.brie_string_cheese.quantity || 0,
  //   magical_string_cheese: quest?.items?.magical_string_cheese.quantity || 0,
  //   marble_string_cheese: quest?.items?.marble_string_cheese.quantity || 0,
  //   mist_canister_stat_item: quest?.items?.mist_canister_stat_item.quantity || 0,
  //   polluted_parmesan_cheese: quest?.items?.polluted_parmesan_cheese.quantity || 0,
  //   polluted_parmesan_potion: quest?.items?.polluted_parmesan_potion.quantity || 0,
  //   swiss_string_cheese: quest?.items?.swiss_string_cheese.quantity || 0,
  //   terre_ricotta_cheese: quest?.items?.terre_ricotta_cheese.quantity || 0,
  //   terre_ricotta_potion: quest?.items?.terre_ricotta_potion.quantity || 0,
  // };

  // console.log({ armedBait, canMist, isMisting, mistPercent, mistLevel, mistTier, items });
};

export default main;
