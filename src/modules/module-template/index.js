import { addUIStyles } from '@/utils';

import styles from './styles.css';

const main = () => {
  // do stuff
};

/**
 * Initialize the module.
 */
export default () => {
  addUIStyles(styles);
  main();
};
